{
  "name": "icare-figma-plugin",
  "version": "1.0.0",
  "description": "Import iCare design tokens, components, and screen layouts into Figma",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "dev": "npm run watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.89.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0"
  }
}
